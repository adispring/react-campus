import React from 'react';

const SearchBar = () => (
  <div className="searchBar">
    <label htmlFor="search">搜索栏:</label>
    <input type="text" id="search" placeholder="搜索..." />
  </div>
);

export default SearchBar;
